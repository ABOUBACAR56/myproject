name: CI/CD - Build & Push Docker Image

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  # Permet de lancer le workflow manuellement depuis l'onglet Actions
  workflow_dispatch:

env:
  IMAGE_NAME: techlogix-inventory

jobs:
  # ── JOB 1 : TESTS ────────────────────────────────────────────────────────
  test:
    name: 🧪 Test Application
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        # npm ci est plus rapide et propre pour les pipelines CI
        run: npm ci || npm install 

      - name: 🔍 Check application syntax
        # On vérifie que le fichier principal n'a pas d'erreur de syntaxe
        run: node --check server.js

  # ── JOB 2 : BUILD & PUSH ─────────────────────────────────────────────────
  build-and-push:
    name: 🐳 Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: test # Le build ne se lance que si les tests réussissent

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔐 Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: 🏷️ Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKER_HUB_USERNAME }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=raw,value=v1.0
            type=sha,prefix=sha-

      - name: 🏗️ Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # Optimisation : utilise le cache natif de GitHub Actions
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: ✅ Summary
        run: |
          echo "Build terminé avec succès pour ${{ env.IMAGE_NAME }}"
